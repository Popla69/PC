# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# ========================================
# PERFORMANCE OPTIMIZATION SETTINGS
# ========================================
# Maximum memory allocation for Gradle JVM (8GB for complex builds)
org.gradle.jvmargs=-Xmx8192m -XX:MaxMetaspaceSize=1024m -XX:+UseG1GC -XX:+UseStringDeduplication -Dfile.encoding=UTF-8

# Parallel processing and caching for faster builds
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# KAPT (Kotlin Annotation Processing) optimizations
kapt.incremental.apt=true
kapt.use.worker.api=true
kapt.include.compile.classpath=false

# Worker optimization for multi-core systems
org.gradle.workers.max=4

# Gradle daemon optimization
org.gradle.daemon=true

# Additional performance settings
org.gradle.vfs.watch=true
org.gradle.unsafe.configuration-cache=true
org.gradle.configuration-cache.problems=warn

# Android specific optimizations
android.enableJetifier=true
android.enableR8.fullMode=false
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# JDK 17 Configuration for Hilt + Room + KAPT Support
# Absolute path for Android Studio compatibility (comment out if using embedded JDK)
org.gradle.java.home=F:\\AgenticAI\\jdk-env\\jdk-17.0.17+10
# For Android Studio: Use embedded JDK by commenting out the line above

# Alternative system-wide JDK 17 paths (uncomment if using system installation)
# org.gradle.java.home=C:\\Program Files\\Java\\jdk-17
# org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.9.9-hotspot